# Unattended Dev System

Auto-generated by unattended-dev-system skill for project: **{{PROJECT_NAME}}**

## Quick Start

```bash
# Start driver
tmux new-session -d -s dev-driver './driver.sh'

# Install watchdog
( crontab -l 2>/dev/null | grep -v watchdog.sh ; \
  echo "*/10 * * * * $(pwd)/watchdog.sh" ) | crontab -

# Monitor
tail -f .unattended/logs/driver.log
```

## Project Configuration

- **Language**: {{LANGUAGE}}
- **Framework**: {{FRAMEWORK}}
- **Test Command**: {{TEST_COMMAND}}
- **Project Root**: {{PROJECT_ROOT}}

## Files

| File | Purpose |
|------|---------|
| `driver.sh` | Main driver loop |
| `watchdog.sh` | Watchdog monitor |
| `orchestrator.py` | Auto orchestrator |
| `.unattended/config.json` | Configuration |
| `.unattended/tasks/` | Task files |
| `.unattended/checkpoints/` | Progress checkpoints |

## Commands

```bash
# View status
python orchestrator.py --status

# Save checkpoint
python orchestrator.py --save <phase> <message> <next> <context>

# List checkpoints
python orchestrator.py --list-checkpoints

# Generate resume instruction
python orchestrator.py --resume

# Check quota
python orchestrator.py --check
```

## Monitoring

```bash
# Driver logs
tail -f .unattended/logs/driver.log

# Orchestrator logs
tail -f .unattended/logs/orchestrator.log

# Watchdog logs
tail -f .unattended/logs/watchdog.log

# Current status
cat .unattended/status.yaml
```

## Configuration

Edit `.unattended/config.json` to customize:

```json
{
  "driver": {
    "providers": ["provider1/model", "provider2/model"],
    "timeout": 7200,
    "zero_output_fuse": 900
  },
  "watchdog": {
    "stuck_threshold_minutes": 45
  },
  "notification": {
    "enabled": true,
    "webhook": "https://..."
  }
}
```

## Stopping

```bash
# Stop driver
tmux kill-session -t dev-driver

# Remove watchdog
crontab -l | grep -v watchdog.sh | crontab -

# Cleanup (optional)
rm -rf .unattended driver.sh watchdog.sh orchestrator.py
```

## Customization

### Adding Custom Task Templates

Create `.unattended/task-template.md` for custom task format.

### Adding Recovery Hooks

Create `.unattended/recovery-hook.sh` for custom recovery logic.

### Adding Notification Webhooks

Configure in `.unattended/config.json`:

```json
{
  "notification": {
    "enabled": true,
    "webhook": "https://hooks.slack.com/...",
    "on_events": ["task_complete", "error", "quota_limit"]
  }
}
```

## Troubleshooting

### Driver won't start

```bash
# Check lock file
ls -la /tmp/{{PROJECT_NAME}}-driver.lock
rm -f /tmp/{{PROJECT_NAME}}-driver.lock
```

### Tests failing

```bash
# Run tests manually
{{TEST_COMMAND}}

# Fix and restart
tmux kill-session -t dev-driver
tmux new-session -d -s dev-driver './driver.sh'
```

### Git push failing

```bash
# Check SSH key
ssh -T git@github.com

# Check git config
git config user.name
git config user.email
```

## Support

- Full documentation: `SKILL.md`
- Configuration: `.unattended/config.json`
- Logs: `.unattended/logs/`

---

**Generated**: {{GENERATED_DATE}}
**Skill Version**: 1.0.0
